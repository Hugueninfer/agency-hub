# API base (ex. http://127.0.0.1:8000). Com proxy do Vite em /api e /sanctum, pode ficar vazio.
# Com domínio cruzado, o browser precisa de CORS (CORS_ALLOWED_ORIGINS no backend) e credenciais.
VITE_API_BASE_URL=
